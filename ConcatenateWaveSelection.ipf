#pragma rtGlobals=3		// Use modern global access method and strict wave access.
Function ConcatenateSelected(List)
string list
Concatenate(List), Waterfall
	//variable i
	//for(i=0;i<itemsinlist(list);i+=1)
		//WAVE Ywave=$stringfromlist(i,list,";")
		
	//endfor
end